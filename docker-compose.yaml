services:
  fsignal:
    build: .
    ports:
      - "3000:3000"
      - "5000:5000"
    environment:
      - FSIGNAL_TCP_PORT=5000
      - SERVER_PORT=3000
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
